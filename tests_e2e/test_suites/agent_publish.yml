#
# This test is used to verify that the agent will be updated after publishing a new version to the agent update channel.
#
name: "AgentPublish"
tests:
  - "agent_publish/agent_publish.py"
images:
  - "random(endorsed, 1)"
  - "random(endorsed-arm64, 1)"
location: "centraluseuap"
owns_vm: true