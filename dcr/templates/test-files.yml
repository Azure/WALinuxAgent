# Template for deleting ARM resources

parameters:
  - name: files
    type: object
    default:
      one.didnt.work

steps:
  - ${{ each value in parameters.files }}:
    - script: |
        echo $(value)