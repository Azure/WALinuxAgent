parameters:
  - name: timesToRun
    type: string

  - name: ymlFilePath
    type: string
    default: ''

steps:
  - script: echo 'yolo this, FTW!'

  - ${{  }}
#  - template: ${{ parameters.ymlFilePath }}